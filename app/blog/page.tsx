import React from "react";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog | Keys Rig",
};

const Blog = () => {
  return <div>Blog</div>;
};

export default Blog;
